# Runtime dependencies for the eodi_classroom pipeline.
# Versions are floored at known-good majors; pin exact versions for a
# fully reproducible environment (e.g. pandas==2.2.2).
pandas>=2.0
numpy>=1.24
openpyxl>=3.1          # .xlsx I/O for pandas
matplotlib>=3.7
seaborn>=0.12
prophet>=1.1           # time-series forecasting (pulls in cmdstanpy)
